import styled from 'styled-components';

const Container = styled.div`
  width: 100%;
  height: 106px;
  background: #f3f3f3;
`;

const Header = () => {
  return <Container>제목 없는 설문지</Container>;
};

export default Header;
